{
  "name": "frontend",
  "version": "0.1.0",
  "private": true,
  "devDependencies": {
    "browser-sync": "^3.0.2",
    "connect-history-api-fallback": "^2.0.0"
  },
  "scripts": {
    "start": "browser-sync start --server --exclude \"./node_modules\" --config bs-config.js  --port 8000 --https"
  },
  "browserslist": {
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
