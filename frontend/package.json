{
  "name": "frontend",
  "version": "0.1.0",
  "private": true,
  "devDependencies": {
    "browser-sync": "^2.29.3",
    "connect-history-api-fallback": "^2.0.0"
  },
  "scripts": {
    "start": "browser-sync start --server --files \"**/*\" --exclude \"./node_modules\" --config bs-config.js --index \"./index.html\""
  },
  "browserslist": {
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
